import React from 'react';

interface CommentType {
  id: number;
  content: string;
  userId: number;
  createdAt: string;
}

const Comment: React.FC = () => {
  return <>댓글 컴포넌트</>;
};

export default Comment;
